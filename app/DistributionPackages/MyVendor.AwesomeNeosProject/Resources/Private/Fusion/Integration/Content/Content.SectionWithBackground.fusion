prototype(MyVendor.AwesomeNeosProject:Content.SectionWithBackground) < prototype(Neos.Neos:ContentComponent) {
    renderer = MyVendor.AwesomeNeosProject:Component.SectionWithBackground {
        backgroundColor = ${q(node).property('backgroundColor')}
        contentWidth = ${q(node).property('contentWidth')}
        padding = ${q(node).property('padding')}

        content = Neos.Neos:ContentCollection {
            nodePath = 'main'
        }
    }
}
